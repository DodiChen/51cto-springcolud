server:
  port: 8771

# 绑定注册中心地址
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

# 服务的名称
spring:
  application:
    name: product-service
  zipkin:
    base-url: http://localhost:9411/
  # 配置 采样百分比
  sleuth:
    sampler:
      probability: 1
